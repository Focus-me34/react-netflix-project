import { propTypes } from "react-bootstrap/esm/Image";
import classes from "./SeparationPattern.module.css"

const SeparationPattern = () => {
  return (
    <div className={classes.separation}></div>
   );
}

export default SeparationPattern;
